[workspace]
members = [
    "simple",
    "vectors",
    "bindings-simple",
    "bindings-vectors",
    "uniffi-bindgen",
]

[workspace.dependencies]
uniffi = { git = "https://github.com/zduny/uniffi-rs", rev = "55ba91c" }
# uniffi = { path = "../uniffi-rs/uniffi" }
